[**modern-react-template v0.0.4**](../../../../README.md)

***

[modern-react-template](../../../../modules.md) / [components/Button/Button.stories](../README.md) / default

# Variable: default

> `const` **default**: `object`

Defined in: [src/components/Button/Button.stories.tsx:4](https://github.com/asudbury/modern-react-template/blob/000be6994f4ac0ab10bd189fe11233a0d8a44ac0/src/components/Button/Button.stories.tsx#L4)

## Type Declaration

### argTypes

> **argTypes**: `object`

#### argTypes.children

> **children**: `object`

#### argTypes.children.control

> **control**: `"text"` = `'text'`

#### argTypes.children.description

> **description**: `string` = `'The content of the button'`

#### argTypes.disabled

> **disabled**: `object`

#### argTypes.disabled.control

> **control**: `"boolean"` = `'boolean'`

#### argTypes.disabled.description

> **description**: `string` = `'Whether the button is disabled'`

#### argTypes.fullWidth

> **fullWidth**: `object`

#### argTypes.fullWidth.control

> **control**: `"boolean"` = `'boolean'`

#### argTypes.fullWidth.description

> **description**: `string` = `'Whether the button should take full width'`

#### argTypes.size

> **size**: `object`

#### argTypes.size.control

> **control**: `"select"` = `'select'`

#### argTypes.size.description

> **description**: `string` = `'The size of the button'`

#### argTypes.size.options

> **options**: `string`[]

#### argTypes.variant

> **variant**: `object`

#### argTypes.variant.control

> **control**: `"select"` = `'select'`

#### argTypes.variant.description

> **description**: `string` = `'The visual style variant of the button'`

#### argTypes.variant.options

> **options**: `string`[]

### component

> **component**: `ForwardRefExoticComponent`\<[`ButtonProps`](../../Button/interfaces/ButtonProps.md) & `RefAttributes`\<`HTMLButtonElement`\>\> = `Button`

### parameters

> **parameters**: `object`

#### parameters.layout

> **layout**: `string` = `'centered'`

### tags

> **tags**: `string`[]

### title

> **title**: `string` = `'Components/Button'`
