import { DefaultTheme } from 'typedoc';

/**
 * Custom TypeDoc theme that removes default items.
 */
export class CustomTheme extends DefaultTheme {
  // We rely on a tiny CSS override to hide the footer element.
}
